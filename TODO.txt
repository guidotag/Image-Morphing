- Menu bar:
	- Preferences. Should open a window with fields:
		- Segments file folder.
		- Morph binary file.
		- Output folder.
	- Help.

- Button to swap images (with its segments).
- Individual segment deletion. We could left-click a segment, mark it in a different color, and finally remove it with supr.
- Load segments file. How do we check the segments are valid given the two loaded pictures?